git config --global user.email "roman.optiplex@gmail.com"
git config --global user.name "roman-optiplex"
cd
git clone https://github.com/tjel/infpro-0.git 
cd infpro-0
echo "roman-optiplex, roman.optiplex@gmail.com" >> team-0.txt
git status
git add team-0.txt
git commit -m "List of members of team-0 added to team-0.txt."
git push


echo "#" >> hello.cc
// 'Hello World!' program 
 
#include <iostream>
 
int main()
{
  std::cout << "Hello World!" << std::endl;
  return 0;
}
g++ hello.cc -o hello
gcc hello.cc -o hello -lstdc++
